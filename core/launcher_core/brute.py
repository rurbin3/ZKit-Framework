def main():
    from colorama import Fore
    from os import path
    red, blue, green, reset = (
        Fore.LIGHTRED_EX,
        Fore.LIGHTBLUE_EX,
        Fore.LIGHTGREEN_EX,
        Fore.RESET,
    )
    yellow, magenta = Fore.LIGHTYELLOW_EX, Fore.LIGHTMAGENTA_EX
    print("So Whats your attack type ???"
    "{1} --> Sending request to login endpoint (recommended)\n"
    "{2} --> Sending request and submitting it from login page")
    CHOICE = str(input(""))
    if CHOICE not in 
    print("Brute Forcing ?? So Choose your web address or enter the direct url to login page (not signup)\n"
          "{1} --> Facebook : https://facebook.com \n"
          "{2} --> Instagram : https://instagram.com \n"
          "{3} --> Github : https://github.com"
          "(Some sites might be filtered in some countries . if is tunnel all your traffic to A VPN and try again . "
          )
    site = str(input("..> "))
    
    if site in ['1','2','3']:
        site = site.replace()
    else :
        password_field = str(input('what is password field named in that website (default is password) left it blank to use  : '))
       


    
